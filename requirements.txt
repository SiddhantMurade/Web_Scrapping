

requests==2.28.2
beautifulsoup4==4.12.0
pandas==2.1.1
numpy==1.25.0
concurrent-log-handler==0.9.0

requests: For sending HTTP requests.
beautifulsoup4: For parsing and extracting data from HTML content.
pandas: For data manipulation and analysis.
numpy: For numerical operations (used indirectly via pandas).
concurrent-log-handler: A utility to help with logging (though this is optional depending on your logging setup).
